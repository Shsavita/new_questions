 #include<bits/stdc++.h>
using namespace std;
int main() {
	string s;
	cin>>s;
	string res="";
	res+=s[0];
	for(int i=1;i<s.size();i++){
		if(s[i] == s[i-1]){
			continue;
		}
		else{
			res+=s[i];
		}
	}
	cout<<res<<endl;
	return 0;
}
